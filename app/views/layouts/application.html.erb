<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Black Pride Beauty</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <meta name="author" content="">

<!--[if lt IE 9]>
<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
<![endif]-->

  <%= stylesheet_link_tag    "application" %>
  <%= stylesheet_link_tag    "bootstrap-buttons" %>
  <%= stylesheet_link_tag    "bootstrap" %>

  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <% unless request.path == '/' %>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </a>
        <%= link_to "Black Pride Beauty", "/", :class => "brand" %>
				<%= yield :nav_links %>
      </div>
    </div>
  </div>
  <div class="container-fluid">
		<%- flash.each do |name, msg| -%>
			<% content = content_tag(:a, '', :class => "close") + content_tag(:p, msg) %>
			<%= content_tag :div, content, :class => "notification msgerror" if (msg.is_a?(String) && flash[:alert].present? ) %>
			<%= content_tag :div, content, :class => "notification msginfo" if (msg.is_a?(String) && flash[:notice].present? ) %>
		<% end %>
<% else %>
  <% end %>
    <%= yield %>

    <hr>
    <footer>
      <center><p>&copy; Black Pride Beauty 2012</p></center>
    </footer>
  </div>

</body>
</html>
